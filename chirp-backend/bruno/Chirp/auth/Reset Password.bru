meta {
  name: Reset Password
  type: http
  seq: 6
}

post {
  url: {{BASE_URL}}/{{AUTH}}/forgot-password
  body: json
  auth: inherit
}

body:json {
  {
    "email": "test1@test.com"
  }
}

settings {
  encodeUrl: true
}
