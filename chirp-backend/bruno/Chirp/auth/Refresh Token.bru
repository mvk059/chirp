meta {
  name: Refresh Token
  type: http
  seq: 3
}

post {
  url: {{BASE_URL}}/api/auth/refresh
  body: json
  auth: inherit
}

body:json {
  {
    "refreshToken": ""
  }
}

settings {
  encodeUrl: true
}
